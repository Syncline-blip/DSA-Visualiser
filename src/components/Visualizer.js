// src/components/Visualizer.js
import React from 'react';

const Visualizer = ({ type }) => {
  return (
    <div>
      <h3>Visualizer for {type}</h3>
      {/* Add actual visualization logic here */}
    </div>
  );
};

export default Visualizer;
