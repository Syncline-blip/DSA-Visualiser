// src/components/Quiz.js
import React from 'react';

const Quiz = ({ topic }) => {
  return (
    <div>
      <h3>{`Quiz on ${topic}`}</h3>
      {/* Add quiz content here */}
    </div>
  );
};

export default Quiz;
